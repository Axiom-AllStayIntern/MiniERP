/**
 * @deprecated Import schema from `src/infrastructure/db` instead.
 *
 * This file remains as a compatibility re-export while callers migrate to the
 * target-layer DB entrypoint.
 */
export * from '../../../infrastructure/db/schema';
