import type { EventBus, ModuleContext } from '$platform/modules/types';

export function registerExpenseHandlers(_bus: EventBus, _ctx: ModuleContext) {
	// Expense module is primarily an event emitter
}
